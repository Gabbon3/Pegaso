# 24. Alberi Binari

## 1. Definizione Sacra

è un albero dove ogni nodo ha 2 figli, solo le foglie non hanno figli.

### Implementazione nodo in C

```c
struct node {
    int data; // il valore intero
    struct node *left; // nodo figlio sinistro
    struct node *right; // nodo figlio destro
    struct node *parent; // nodo padre
}
```

## 3. Inserimneto

L inserimento avviene gia in maniera ordinata, precursione degli alberi ordinati BST:

```c
if (data <= temp->data) {
    // inserisci il dato a sinistra
} else {
    // inserisci il dato a destra
}
```

## 4. Visualizzazione (Recursion Power)

per stampare un albero binario non ce meglio che la ricorsione fino alle foglie i quali puntatori ai figli sono null.

